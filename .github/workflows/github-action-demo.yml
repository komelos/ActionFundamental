name: Github Action Demo
on:
  push:
    branches:
      - 'main'
    paths-ignore: .github
  pull_request:
    branches: 'main'
  schedule:
    - cron: '15 6 * * 0'
  workflow_dispatch:

jobs:
  demo:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hej BMW 🚙"
        
